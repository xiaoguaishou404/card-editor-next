version: '3.8'

services:
  card-editor:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    container_name: card-editor-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev 